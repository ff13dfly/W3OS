/* 
*  W3OS iNFT decoder
*  @auth [ Fuu ]
*  @creator Fuu
*  @date 2024-03-13
*  @functions
*  1.decode the iNFT definition on hash.
*/

const iNFT = {
  
    //decode the iNFT from hash
    //create a bs64 image file
    //return ck ( BASE64_RAW_FILE, FILE_TYPE )
    decode:(def,hash,ck)=>{

    },

    //Load definition from "uri"
    //type of "uri", could be [ anchor_link, IPFS_link, Accounts_address ]
    load:(uri,type,ck)=>{

    },
};

export default iNFT;
